@page
@model KOTApp.Pages.costChanges.EditModel

@{
    ViewData["Title"] = "Edit";
}

<h1>Edit</h1>

<h4>Edit Change Order</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form method="post">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <input type="hidden" asp-for="CostChange.ChangeId" />
            <div class="form-group">
                <label asp-for="CostChange.Cost" class="control-label"></label>
                <input asp-for="CostChange.Cost" class="form-control" />
                <span asp-validation-for="CostChange.Cost" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="CostChange.Description" class="control-label"></label>
                <input asp-for="CostChange.Description" class="form-control" />
                <span asp-validation-for="CostChange.Description" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input type="submit" value="Save" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-page="./Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
